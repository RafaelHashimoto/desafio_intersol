table
  thead
    tr
      th Tipo
      th Data
      th Valor
  tbody
    - @account_transactions.each do |transaction|
      tr 
        td #{transaction.t_enum("transaction_type")}
        td #{transaction.date}
        td #{transaction_amount(transaction, @account)}
  